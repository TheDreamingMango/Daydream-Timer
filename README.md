# Stop Daydreaming

Minimal Flutter app.

## Run

```bash
flutter pub get
flutter run -d chrome
```

Requires Flutter (`brew install --cask flutter`). Chrome works today; full macOS/iOS needs Xcode, Android needs Android Studio.
